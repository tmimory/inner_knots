/**
 * Moved to `components/ui/field.tsx`.
 *
 * `Field` and `FormSection` started here, on the character editor, and turned out
 * to be the rhythm every form in the app wants. They live in the UI kit now; this
 * file stays so `@/components/characters` keeps answering for them.
 */
export {
  Field,
  FormSection,
  SectionHeading,
  type FieldProps,
  type FormSectionProps,
  type SectionHeadingProps,
} from "@/components/ui/field";
