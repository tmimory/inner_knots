/**
 * What every floating surface wears: the popover fill and the hairline that
 * separates it from whatever it hovers over. Tooltips, select menus, the trolley
 * board's object popover and the palette's tap-to-place menu each add their own
 * radius, padding and elevation; the fact that they are all one kind of surface
 * is said once, here.
 */
export const POPOVER_SURFACE_CLASSES = "border-hairline border-border bg-popover";
