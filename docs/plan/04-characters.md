# Phase 4 — Characters

## Checklist
- [ ] `components/avatars/`: 15 SVG shapes (react-native-svg) each exposing one `color` prop that tints a meaningful region (shirt, feathers, shell…), 25 named colors in `theme/tokens.ts` under `avatarPalette`.
- [ ] Characters list: avatar grid with names; search by identifier, provider, model, output mode, steering mode.
- [ ] Editor: identifier (no spaces, uniqueness check, UUID default), avatar shape + color pickers, provider (enabled only), model (from provider), output mode toggle (tool disabled when unsupported), effort (when supported), steering mode with bio / principles / values as removable chips (limits enforced), live Final Prompt preview from `/api/prompts`.
- [ ] Delete with confirmation.

## Done
_(fill in on completion)_
